# Optional: machine learning and deep learning support for Luna
# Install with: pip install -r requirements-ml.txt
# Enables the !ml tool (train/predict with scikit-learn) and lets Luna use ML in scripts.

scikit-learn>=1.0.0
joblib>=1.0.0
numpy>=1.20.0

# Optional: deep learning (for writing PyTorch scripts; not required for !ml tool)
# torch>=2.0.0

# Optional: Voice ID (podcast studio + hub /api/transcribe speaker gate) — Resemblyzer + torch.
# Plain `pip install resemblyzer` fails on Windows + Python 3.13: it tries to compile `webrtcvad` (needs full MSVC + SDK).
# Use prebuilt VAD wheels, then install Resemblyzer without pulling the broken dependency:
#   pip install webrtcvad-wheels
#   pip install resemblyzer --no-deps
# Ensure torch, librosa, scipy, numpy are already installed (Whisper stack usually covers this).
