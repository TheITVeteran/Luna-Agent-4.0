{
  "name": "dict_case_everywhere_except_first_last_a",
  "description": "Prints a JSON object with all keys and values in title case, except for the first key-value pair which is in lowercase",
  "script": "import json, sys\nparams = json.load(sys.stdin)\nresult = {k.lower(): v for k, v in params.items()[1:]}\nprint(result)"
}